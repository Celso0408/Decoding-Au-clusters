# SketchMapPy Dependencies
# Install via: pip install -r requirements.txt
# Or install the framework directly: pip install -e .

numpy>=1.24
scipy>=1.11
scikit-learn>=1.3
matplotlib>=3.7
pymbar>=4.0

# Optional GPU acceleration (install if needed)
torch>=2.0
cuml-cu12 # (via NVIDIA channel)

# Optional dependencies for modern embeddings
openTSNE>=1.0
umap-learn>=0.5

# Testing
# pytest>=9.0
